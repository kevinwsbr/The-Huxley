#include <stdio.h>

int main(int argc, char const *argv[])
{
	char s[8];
	char _s[8];
	int m = 0, r = 0, w = 0 i = 0;

	while((m != 5) && (r != 5))
	{
		fgets(8, s, stdin);
		switch(s[2])
		{
			case 'd':

			break;
			case 'p':
			break;
			case 's':
			break;
		}
		if (!flag)
		{
			/* code */
		}
	}

	return 0;
}